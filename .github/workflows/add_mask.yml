name: Add mask
# on: push
on: workflow_dispatch
jobs:
  log:
    runs-on: ubuntu-latest
    env:
      PASSWORD: SuperSecretValue
    steps:
      # mask the PASSWORD when displaying the log
      - run: echo "::add-mask::${PASSWORD}"
      - run: echo "${PASSWORD}"