name: JSON functions
# on: push
on:
  workflow_dispatch:
jobs:
  dump:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${CONTEXT}"
        env:
          # dump github context as JSON
          CONTEXT: ${{ toJSON(github) }}